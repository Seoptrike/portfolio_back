<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.backend.mapper.WorkExperienceMapper">
    <select id="findWorkExpByUserId" resultType="com.backend.domain.career.WorkExperiencesVO">
        SELECT *
        FROM work_experiences
        WHERE user_id = #{userId}
        ORDER BY end_date DESC NULLS FIRST, start_date DESC
    </select>

    <insert id="insertWorkExp">
        INSERT INTO work_experiences (user_id, company_name, position, start_date, end_date)
        VALUES (#{userId}, #{companyName}, #{position}, #{startDate}, #{endDate})
    </insert>

    <update id="updateWorkExp">
        UPDATE work_experiences
        SET company_name = #{companyName} ,
            position = #{position},
            start_date = #{startDate},
            end_date = #{endDate}
        WHERE work_id = #{workId}
    </update>

    <delete id="deleteWorkExp">
        DELETE
        FROM work_experiences
        WHERE work_id = #{workId}
    </delete>
</mapper>
